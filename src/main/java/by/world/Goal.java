package by.world;

public class Goal extends Entity{

    public Goal(Position position) {
        super(position, EntityType.GOAL);
    }

    @Override
    public Position getPosition() {
        return super.getPosition();
    }
}
